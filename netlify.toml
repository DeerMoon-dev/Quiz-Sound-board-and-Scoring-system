[build]
  base = ""            # set to your subdirectory if your project is not at repo root, otherwise ""
  publish = "dist"     # or "build" / the folder your build outputs
  command = "npm run build"
